# Frontend (Vite) ---------------------------------------------------------
# Defina como "false" para usar as Netlify Functions reais em vez do mock.
VITE_USE_MOCK=true

# Backend (Netlify Functions) ----------------------------------------------
# Chave do Google AI Studio (free tier). NUNCA versionar a chave real.
GEMINI_API_KEY=

# Obs.: o armazenamento (documentos, trechos e embeddings) usa Netlify Blobs,
# que é provisionado automaticamente — não há banco externo nem connection string.
